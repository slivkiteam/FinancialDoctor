export type LoginData = {
    accessToken: string;
    refreshToken: string;
    id: number;
    username: string;
}

export type RegisterData = {
    name: string;
    email: string;
    phoneNumber: string;
    password: string;
}

